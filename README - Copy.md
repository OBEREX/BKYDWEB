
  # Build in React

  This is a code bundle for Build in React. The original project is available at https://www.figma.com/design/jcx6jOAZajTfx5o9oUjVG9/Build-in-React.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  